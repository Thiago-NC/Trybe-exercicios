const hof = (corretas,verificadas,check) => {
    return 'total de respostas certas';
};

const RIGHT_ANSWERS = ['A', 'C', 'B', 'D', 'A', 'A', 'D', 'A', 'D', 'C'];
const STUDENT_ANSWERS = ['A', 'N.A', 'B', 'D', 'A', 'C', 'N.A', 'A', 'D', 'B'];

const contarPontos = () => {
    let contador = 0;

    if (contador === RIGHT_ANSWERS){

        for (let i=0; i < RIGHT_ANSWERS.length; i+=1);
        contador += contador[i]
    } else if (contador === )
    console.log(contador)
};
